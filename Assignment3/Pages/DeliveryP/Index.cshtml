@page
@model Assignment3.Pages.DeliveryP.IndexModel


@{

    ViewData["Title"] = "Index";
}

<h1>Orders</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DeliveryPerson[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.DeliveryPerson) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DPersonID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DPersonID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DPersonID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
